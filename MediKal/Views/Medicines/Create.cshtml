@{
    ViewBag.Title = "Create";
    Layout = "~/Views/Shared/_ManagementLayout.cshtml";
}


@using (Html.BeginForm("AddImage","Medicines")) 
{
    @Html.AntiForgeryToken()
 <br />   
 <br />   
<div class="row">
    <div class="col-sm-1 col-md-1 col-lg-4 col-xl-4"></div>
    <div class="col-sm-10 col-md-10 col-lg-4 col-xl-4">
        <img src="~/images/ampoule.svg" height="100" width="100" />
        <h4>Add Medicine</h4>
        <br />
        <div class="form-group">
            <div>
                @Html.TextBox("id", null, new { placeholder = "Enter NDCId" })
                @Html.Label(Session["Message"].ToString(), new { @class = "text-danger" })
            </div>
        </div>
        <div class="form-group">
            <div>
                <input type="submit" value="search" class="btn blue-btn" />
                <a href="~/Medicines" class="btn green-btn">Back to List</a>
            </div>
        </div>

    </div>
</div>
    }

